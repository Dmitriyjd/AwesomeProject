import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    minHeight: 10,
    minWidth: 40,
    height: 38,
    width: 35,
    maxHeight: 300,
    maxWidth: 300,
    resizeMode: 'center',
  },
});

export default styles;
